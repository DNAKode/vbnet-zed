{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VB.NET netcoredbg debug configuration",
  "type": "object",
  "required": ["request"],
  "properties": {
    "name": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "const": "netcoredbg"
    },
    "request": {
      "type": "string",
      "enum": ["launch", "attach"]
    },
    "program": {
      "type": "string",
      "description": "Path to the compiled .NET assembly to launch."
    },
    "projectPath": {
      "type": "string",
      "description": "Path to a .vbproj or project directory used by VB.NET tooling to infer the program."
    },
    "args": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "cwd": {
      "type": "string"
    },
    "env": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "stopAtEntry": {
      "type": "boolean",
      "default": false
    },
    "justMyCode": {
      "type": "boolean",
      "default": true
    },
    "enableStepFiltering": {
      "type": "boolean",
      "default": true
    },
    "configuration": {
      "type": "string",
      "description": "Build configuration used with projectPath convenience launch settings.",
      "default": "Debug"
    },
    "framework": {
      "type": "string",
      "description": "Target framework used with projectPath convenience launch settings."
    },
    "buildBeforeLaunch": {
      "type": "boolean",
      "description": "Whether a project-based launch should build before debugging.",
      "default": false
    },
    "processId": {
      "type": "integer"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "request": {
            "const": "launch"
          }
        }
      },
      "then": {
        "anyOf": [
          {
            "required": ["program"]
          },
          {
            "required": ["projectPath"]
          }
        ]
      }
    },
    {
      "if": {
        "properties": {
          "request": {
            "const": "attach"
          }
        }
      },
      "then": {
        "required": ["processId"]
      }
    }
  ]
}
